// <auto-generated/>
using System;
using System.Buffers;
using lib.utils.span;

namespace plugins.regexgrep;

partial struct GrepMatchData
{
    public (IMemoryOwner<char> Owner, int Length) ToJson()
    {
        var owner = MemoryPool<char>.Shared.Rent(1024);
        var span = owner.Memory.Span;
        int pos = 0;

        try
        {
            "{".AsSpan().AppendTo(span, ref pos);
            "\"Path\":\"".AsSpan().AppendTo(span, ref pos);
            if (!Path.IsEmpty) Path.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"PreMatch\":\"".AsSpan().AppendTo(span, ref pos);
            if (!PreMatch.IsEmpty) PreMatch.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"Match\":\"".AsSpan().AppendTo(span, ref pos);
            if (!Match.IsEmpty) Match.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"PostMatch\":\"".AsSpan().AppendTo(span, ref pos);
            if (!PostMatch.IsEmpty) PostMatch.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"LineNumber\":".AsSpan().AppendTo(span, ref pos);
            LineNumber.AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"Column\":".AsSpan().AppendTo(span, ref pos);
            Column.AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"MatchLength\":".AsSpan().AppendTo(span, ref pos);
            MatchLength.AppendTo(span, ref pos);
            "}".AsSpan().AppendTo(span, ref pos);
            '\n'.AppendTo(span, ref pos);
            return (owner, pos);
        }
        catch (ArgumentOutOfRangeException)
        {
            owner.Dispose();
            throw new InvalidOperationException("Buffer troppo piccolo per il JSON");
        }
    }

    public (IMemoryOwner<char> Owner, int Length) ToCsv()
    {
        var owner = MemoryPool<char>.Shared.Rent(1024);
        var span = owner.Memory.Span;
        int pos = 0;

        try
        {
            Path.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            PreMatch.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            Match.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            PostMatch.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            LineNumber.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            Column.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            MatchLength.AppendTo(span, ref pos);
            '\n'.AppendTo(span, ref pos);
            return (owner, pos);
        }
        catch (ArgumentOutOfRangeException)
        {
            owner.Dispose();
            throw new InvalidOperationException("Buffer troppo piccolo per il CSV");
        }
    }
}
