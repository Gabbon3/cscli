// <auto-generated/>
using System;
using System.Buffers;
using lib.utils.span;

namespace plugins.find;

partial struct FindResultData
{
    public (IMemoryOwner<char> Owner, int Length) ToJson()
    {
        var owner = MemoryPool<char>.Shared.Rent(1024);
        var span = owner.Memory.Span;
        int pos = 0;

        try
        {
            "{".AsSpan().AppendTo(span, ref pos);
            "\"Directory\":\"".AsSpan().AppendTo(span, ref pos);
            if (!Directory.IsEmpty) Directory.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"Name\":\"".AsSpan().AppendTo(span, ref pos);
            if (!Name.IsEmpty) Name.AppendTo(span, ref pos);
            "\"".AsSpan().AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"IsDirectory\":".AsSpan().AppendTo(span, ref pos);
            IsDirectory.AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"SizeBytes\":".AsSpan().AppendTo(span, ref pos);
            SizeBytes.AppendTo(span, ref pos);
            ",".AsSpan().AppendTo(span, ref pos);
            "\"LastWriteUnixTimeMs\":".AsSpan().AppendTo(span, ref pos);
            LastWriteUnixTimeMs.AppendTo(span, ref pos);
            "}".AsSpan().AppendTo(span, ref pos);
            '\n'.AppendTo(span, ref pos);
            return (owner, pos);
        }
        catch (ArgumentOutOfRangeException)
        {
            owner.Dispose();
            throw new InvalidOperationException("Buffer troppo piccolo per il JSON");
        }
    }

    public (IMemoryOwner<char> Owner, int Length) ToCsv()
    {
        var owner = MemoryPool<char>.Shared.Rent(1024);
        var span = owner.Memory.Span;
        int pos = 0;

        try
        {
            Directory.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            Name.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            IsDirectory.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            SizeBytes.AppendTo(span, ref pos);
            ';'.AppendTo(span, ref pos);
            LastWriteUnixTimeMs.AppendTo(span, ref pos);
            '\n'.AppendTo(span, ref pos);
            return (owner, pos);
        }
        catch (ArgumentOutOfRangeException)
        {
            owner.Dispose();
            throw new InvalidOperationException("Buffer troppo piccolo per il CSV");
        }
    }
}
